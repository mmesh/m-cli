package auth

/*
import (
	"os"

	"mmesh.dev/m-cli/pkg/auth"
	"mmesh.dev/m-lib/pkg/errors"
	"mmesh.dev/m-lib/pkg/utils/msg"
)

func (api *API) Token() {
	if err := auth.UserLogin(false); err != nil {
		msg.Errorf("Unable to login: %v", errors.Cause(err))
		os.Exit(1)
	}

	if err := auth.GetAuthToken(); err != nil {
		msg.Errorf("Unable to get authToken: %v", errors.Cause(err))
		os.Exit(1)
	}
}
*/
